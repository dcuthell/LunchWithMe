import loginRequest from './login-user';
import signupRequest from './signup-user';
import setUserCoords from './set-user-coords';

const ActionCreators = {
  loginRequest,
  signupRequest,
  setUserCoords,
};

export default ActionCreators;
